name: Generate Report on Schedule

# ✅ هذا التريجر يشغّل الـ workflow تلقائيًا كل يوم الساعة 7 صباحًا UTC
on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:  # يسمح لك تشغيله يدويًا من Actions لو حابة تختبرينه

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run PowerShell script to generate report
        run: pwsh ./generate-report.ps1

      - name: Commit report
        run: |
          git config --global user.name "Ashjan"
          git config --global user.email "ashjan@example.com"
          git add report.md
          git commit -m "📄 Updated report from GitHub Action (scheduled)"
          git pull --rebase
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/Ashmoh00/DevOpsDemo.git HEAD:main
